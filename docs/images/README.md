# Public image placeholders

No developer screenshots are committed for `v0.5.0-beta.1`. Before publication, create reviewed captures for the Windows status page, pairing page, Android home, Android notification, and architecture overview.

Every capture must use a fictional device, the placeholder address `<PRIVATE_IPV4>`, synthetic event text, and an intentionally unusable QR graphic. It must contain no real Token, username, timestamp, desktop content, path, notification text, or scannable pairing data.
